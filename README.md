# simpliq Offertenrechner

Dynamischer Managed-Service-Offertenrechner mit Next.js, TypeScript, Tailwind CSS und Recharts.

```bash
npm install
npm run dev
```

Die Preise können in `src/data/pricing-catalog.json` bearbeitet werden.
